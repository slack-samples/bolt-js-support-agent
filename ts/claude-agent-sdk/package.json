{
  "name": "bolt-ts-support-agent-claude",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/app.js",
  "license": "MIT",
  "scripts": {
    "start": "npx tsx app.ts",
    "dev": "npx tsx --watch app.ts",
    "build": "tsc",
    "build:watch": "tsc -w",
    "lint": "npx @biomejs/biome check .",
    "lint:fix": "npx @biomejs/biome check --write .",
    "test": "node --test"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.2.0",
    "@slack/bolt": "^4.6.0",
    "dotenv": "~17.3.1",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.0",
    "typescript": "^5.9.0",
    "tsx": "^4.21.0"
  }
}
